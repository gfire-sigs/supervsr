//go:build amd64 && !goexperiment.simd

#include "textflag.h"

TEXT ·aegisUpdateAVX(SB), NOSPLIT, $0-24
	MOVQ state+0(FP), AX
	MOVQ input0+8(FP), BX
	MOVQ input1+16(FP), CX

	VMOVDQU   0(AX), X0
	VMOVDQU  16(AX), X1
	VMOVDQU  32(AX), X2
	VMOVDQU  48(AX), X3
	VMOVDQU  64(AX), X4
	VMOVDQU  80(AX), X5
	VMOVDQU  96(AX), X6
	VMOVDQU 112(AX), X7

	VAESENC X0, X7, X8
	VAESENC X1, X0, X9
	VAESENC X2, X1, X10
	VAESENC X3, X2, X11
	VAESENC X4, X3, X12
	VAESENC X5, X4, X13
	VAESENC X6, X5, X14
	VAESENC X7, X6, X15

	VPXOR (BX), X8, X8
	VPXOR (CX), X12, X12

	VMOVDQU X8,   0(AX)
	VMOVDQU X9,  16(AX)
	VMOVDQU X10, 32(AX)
	VMOVDQU X11, 48(AX)
	VMOVDQU X12, 64(AX)
	VMOVDQU X13, 80(AX)
	VMOVDQU X14, 96(AX)
	VMOVDQU X15, 112(AX)
	VZEROUPPER
	RET
